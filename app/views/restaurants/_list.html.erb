<div id="restaurants">
  <% @restaurants.each do |restaurant| %>
    <%= render restaurant %>
    <p>
      <%= link_to "See details", restaurant, class: "btn btn-primary" %>
    </p>
  <% end %>
</div>
